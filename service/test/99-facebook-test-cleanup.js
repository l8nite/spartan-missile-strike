var should = require("should");
var fbgraph = require('fbgraph');

var fbconf = require("../lib/facebook-config.js");

after(function(done) {
    console.log("TODO: delete facebook test user");
    done();
});
